# Keep RTT enabled by prj.conf and mirror Zephyr logs to UART20, exposed as the
# onboard CMSIS-DAP virtual COM port. The Sense overlay disables GPS/UART21.
CONFIG_UART_CONSOLE=y
CONFIG_LOG_BACKEND_UART=y
